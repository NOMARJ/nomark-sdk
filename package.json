{
  "name": "nomark-sdk",
  "private": true,
  "workspaces": [
    "packages/engine",
    "packages/pro",
    "packages/create-nomark-engine"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  }
}
